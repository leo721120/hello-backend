info:
  version: 1.2.3
  title: "testonly"

paths:
  /foo/{id}:
    get:
      responses:
        200:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/data"
              example: { id: "id-testonly" }
  /foo:
    post:
      responses:
        200:
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/data"

components:
  schemas:
    data:
      additionalProperties: false
      type: object
      properties:
        id:
          type: string
        a:
          type: string
